# PROGRESS-2026-08-30 — `map:` namespace + stdlib-load parser gaps

Branch: `feature/wheres-extra` @ `216199e` (invariant intact: local == origin/feature/wheres-extra).
Work tree modified (NOT yet committed): `evaluator.rs`, `lexer.rs`, `lib.rs`, `parser.rs`, new `kap-core/src/map.rs`.
Vendored stdlib (`kap-stdlib/std/output3.kap`) is **unmodified** by this session.

## 1. `map:` namespace — FULLY IMPLEMENTED & ORACLE-VERIFIED

Kap's `map:` functions are now native in the port, matching the Kotlin ground truth
(`MapWithFunction` / `MapGetFunction` / `MapRemoveFunction` / `MapEntriesFunction` /
`MapSizeFunction` / `MapKeysFunction` in `org.dhsdevelopments.kap.functions.map`).

### Files
- **`kap-core/src/map.rs`** (NEW, 145 lines): `KapMap { entries: Vec<(Rc<APLValue>, Rc<APLValue>)> }`
  with ordered insert + type-qualified key equality (`values_key_equal`, mirroring Kotlin
  `makeTypeQualifiedKey`: number/char/string/symbol all distinct). Helpers: `lookup`,
  `with_pair`, `with_pairs`, `without`, `to_array` (rank-2 `[[k v] …]`), `keys_array`.
- **`kap-core/src/evaluator.rs`**
  - `APLValue::Map(KapMap)` variant added (lib.rs + exhaustively handled: `format_*`,
    `truthy`, `apl_to_instr`, eval match).
  - Six native arms `eval_apply` (evaluator.rs:1972 `map:with`, :1992 `map:get`,
    :2020 `map:remove`, :2043 `map:entries`, :2050 `map:size`, :2057 `map:keys`).
  - `map:get` returns the **bare value** on hit (`Ok(v)`) and `APLValue::Null` on miss
    (Kotlin `lookupValue(key) ?: APLNilValue`, NOT enclosed scalar).
  - `map:get`/`map:remove` accept BOTH the explicit `L f R` form (`m map:get k`) AND the
    stranded-pair form (`map:get m k` → right is a 2-element array, split into map+key).
  - `map:with`: monadic builds from a key-value array (rank-1 even / rank-2 2-col);
    dyadic `m map:with pairs` updates.
  - Two-gate registration: `map:*` added to `evaluator::is_primitive_name` and
    `parser::is_known_fn`'s namespaced-native allow-list (evaluator.rs:3297).
- **`kap-core/src/lib.rs`**: `APLValue::Map` variant + exhaustive-match handling
  (`class_name`, `format_*`, `truthy`).

### Verified against the Real Kap oracle (JVM, `~/Apps/array/kap-jvm-text`)
| expression | port | oracle |
|---|---|---|
| `m←map:with "a" 1 "b" 2` | `map[size=2]` | `map[size=2]` |
| `map:get m "a"` | `1` (bare) | `1` |
| `map:get m "z"` (miss) | `⍬` | `⍬`/`null` |
| `map:size m` | `2` | `2` |
| `map:keys m` | `(keys)` | `(keys)` |
| `map:entries m` | `("x" 10 "y" 20)` rank-2 | same |
| `map:size (map:remove m "x")` | `1` | `1` |
| `m map:with "p" 9` (explicit dyadic) | updates | updates |

## 2. Parser gaps fixed (unblocking stdlib load)

These were PRE-EXISTING general parser bugs, surfaced when loading the vendored stdlib
(`output3.kap` uses `map:with` with `λ`-function-value pairs). Fixed to match oracle
behavior, verified with direct oracle transcripts.

- **`lexer.rs` backtick continuation** (lexer.rs:50-71): previously emitted a `Newline`
  token after `` ` ``, which broke operand attachment (`map:with \` "a" 1` parsed as a
  bare value). Now joins lines with NO separator token (Kotlin strips the backtick and
  concatenates source). Verified: `` typeToFormatter ← map:with ` `` + continuation parses.
- **`parser.rs` `λ` in strand position** (`is_strand_operand`, parser.rs:3148): `LambdaToken`
  now a strand operand. Oracle: `'a λ{⍵} 'b` → `<a function b>`, `5 λ{⍵×2} 10` → `<5 function 10>`.
- **`parser.rs` `λ` valence in `L f R`** (parse_apply dyadic block, parser.rs:2770+):
  a `λ`-reference following a value now STRIPS as a function value (→ strand) instead of a
  dyadic apply. Fixes `map:with 'kap:x λrenderY` (the `output3.kap` typeToFormatter form).
- **`parser.rs` `⍞(` dynamic function ref** (ApplyToken arm, parser.rs:471 + parse_primary
  arm, parser.rs:5211): `⍞(fnExpr)` now parses the group as a function expression
  (oracle: `⍞(+)` yields the `+` function). Handles the 3 `⍞(` occurrences in the stdlib.

### Result
`output3.kap` now parses **36/37** statements. The stdlib loads with ONE benign warning
(the lone failure, `output3.kap:323`, is the `formatInt` number-formatter display fn).

## 3. Known residual (OUT OF SCOPE — not `map:`-related)

- **`output3.kap:323** `res ← ⍺ ⍞((null≡renderer) ⌷ renderer λ{<⍕⍵}) v` fails with
  `expected ) after ⍞(…)`. Root cause: `parse_function_expr` does not accept a *value-group*
  `(null≡renderer)` as the left operand of the `⌷` operator inside `⍞(…)` — a deep, general
  valence gap in `parse_function_expr`, independent of `map:` and of the `λ`/`⍞(` fixes above.
  The `formatInt` function is a display formatter; core stdlib + all `map:` functions work.
  Fixing it would require a `parse_function_expr` rework (value-left-operand-of-operator) —
  deferred; tracked here for visibility.

## 4. Gates (all green, no regressions)
- `cargo build -p kap-cli` — clean.
- `cargo test -p kap-core --lib` — **96 passed; 0 failed**.
- `cargo test -p kap-core --test conformance curated_kap_parity` — **1 passed; 0 failed**.
- `cargo test -p kap-core --test conformance run_kotlin_conformance` (broad sweep, 2547
  cases) — **passed; 191 mismatches** (identical baseline; `map:` cases are NOT in the
  mismatch set — verified). `MapTest.kt` reports 17/30 "unsupported" but that is the
  conformance harness's JSON-`{}`-map test, distinct from the Kap `map:` namespace; not
  affected by this work.
- Note: broad sweep needs `ulimit -v unlimited` (or high cap); the default 4 GB virtual
  limit aborts with a 128-byte alloc failure on the full sweep.

## 5. Next steps (per roadmap process law)
- [ ] Fix `parse_function_expr` value-left-operand-of-operator to clear the last `output3.kap` warning (optional, out of scope).
- [ ] `git commit` the `map:` + parser fixes; restore branch invariant (`main == strings == feature/wheres-extra`).
