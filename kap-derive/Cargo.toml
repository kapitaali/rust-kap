[package]
name = "kap-derive"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Proc-macro for #[kap_fn] — generates NativeFn impl + inventory registration"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
