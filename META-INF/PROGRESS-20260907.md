# PROGRESS-20260907.md — rust-kap feature/wheres-extra session (2026-09-07)

> Per-day file per ROADMAP §0.1 law 8. Today is 2026-09-07 (EEST).
> Previous day: `META-INF/PROGRESS-20260906.md` (closed-env §2.8a at 68e66bc,
> 89.7%, 0 MISMATCH + `catch` a4 survey with diagnosed-not-applied fix).

**Branch:** `feature/wheres-extra` (HEAD `68e66bc` at session start; dirty tree =
the `catch` work-in-progress surveyed in PROGRESS-20260906 §"a4 survey").

---

## `catch` cluster (§2.8a task a4, picked first) — IMPLEMENTED, VERIFIED

Exactly one fix, in `catch_apply` (`kap-core/src/evaluator.rs:14998`), as
diagnosed in the 20260906 survey: the matched handler was converted via
`value_to_instr` → `Instr::Value` and passed in fn position to `eval_apply`,
which has no `Value` arm → `only symbol/lambda functions supported yet`.
The fix matches the handler `APLValue` directly (mirroring the `DynamicRef`
arm at `evaluator.rs:2646`):

- `UserFn{params, split, body, env: fenv}` → `apply_user_fn(params, split, body,
  Some(data_instr), key_instr, env, fenv, None)`
- `Escape{target}` → `apply_escape(target, ...)`
- `NonBoundFn{body}` → `apply_nonbound_fn(body, ...)`
- anything else → `catch: The handler is not callable, this is currently an error.`

`Instr::Value` wrapping kept ONLY for the data/key *args* (⍺=data, ⍵=key).
`Block` bodies NOT routed through `eval_apply` (revert warning honoured).

Prior uncommitted diff (from the survey) also landed in this commit, unchanged:
`lib.rs` `AplError::Thrown(key, data)` control-flow signal, `throw` arms raise
it (monadic key = `Symbol{error, kap}`), `catch` in `is_adverb`, `catch_apply` +
`symbols_total_equal`, `Derived` dispatch arm, top-level `Thrown → throw: <data>`
in `eval_string_in_env` + `kap-cli/src/main.rs` renderer, RED test
`catch_operator_matrix_and_single_handlers`.

### Oracle matrix (kap-jvm-text, UTF-8, `⋄` name-bound forms — bare `{…}catch` in
leading position is a separate pre-existing gap, NOT this cluster)

| expr (port = oracle) | result |
|---|---|
| `f ⇐ {'foo throw 1} ⋄ f catch 1 2 ⍴ 'foo λ{2+⍺}` | `3` / `⊢ 3` |
| `f ⇐ {'foo throw 1} ⋄ f catch 'foo λ{2+⍺}` | `3` |
| `f ⇐ {'foo throw 1} ⋄ f catch 4 2 ⍴ 'xyz λ{2+⍺} 'test123 λ{3+⍺} 'bar λ{4+⍺} 'foo λ{5+⍺}` | `6` / `⊢ 6` |
| `f ⇐ {1+2} ⋄ f catch 'foo λ{2+⍺}` (no-throw passthrough) | `3` |
| `f ⇐ {'foo throw 1} ⋄ f catch 1 2 ⍴ 'bar λ{2+⍺}` (unmatched) | port `error: throw: 1`, oracle `Error at: 1:7: throw: 1` (rethrow, fails-kind OK) |
| `f ⇐ {'foo throw 1} ⋄ f catch 3 ⍴ …` (odd dims) | port `error: catch: Invalid dimensions of catch argument` |

### Gates (all GREEN, re-run this session)

- `cargo test -p kap-core --lib` → **100/0** (incl. RED test, now green).
- `cargo test -p kap-core --test conformance curated_kap_parity` → **1/0**.
- `cargo test -p kap-core --tests` → all 15 suites green.
- Release sweep (`CONFORM_DUMP=1 CONFORM_DUMP_PATH=$PWD/work/dump-after-catch-fix.txt
  cargo test -p kap-core --test conformance run_kotlin_conformance --release`):
  **2381 OK / 0 MISMATCH / 269 UNSUPPORTED = 89.8%** (2650 cases, 4198 dump lines).
- Positional diff vs `work/dump-after-closedenv-fix.txt` (dump row *i* ↔ jsonl
  case *i*, `LC_ALL=C`): **zero `OK→!OK`**, exactly 4 wins — all ExceptionsTest,
  all with non-empty `expected` (genuine, not coverage flips):
  `simpleException` (EXP 3), `simpleExceptionWithSingleDimensionCatch` (EXP 3),
  `multipleTagHandlers` (EXP 6), `multipleTagHandlersSingleDimensionCatch` (EXP 6).

### Sweep-discipline note (new pitfall)

`CONFORM_DUMP_PATH` must be ABSOLUTE — `work/…` relative resolves against the
test binary's cwd, not the repo root, and silently writes nowhere (first attempt
produced no dump file, exit=101 confusion). Prior session's `CONFORM_DUMP_PATH=
work/…` note in PROGRESS-20260906 assumed repo-root cwd; use `$PWD/work/…`.
Dump also still appends — `rm -f` first.

### Files

`kap-core/src/evaluator.rs` (`catch_apply` match arms + RED test),
`kap-core/src/lib.rs` (`AplError::Thrown`), `kap-core/src/parser.rs`
(`catch` in `is_adverb`), `kap-cli/src/main.rs` (uncaught-`Thrown` render),
`work/dump-after-catch-fix.txt` + `work/sweep-catch.log` (git-ignored scratch).

---

## Next (queued, NOT started)

- AssignmentTest nested-destructure (4 rows: `((a b) c) ← (1 2) 3` etc. → oracle
  `⟨1 2 3⟩`; needs parser shape change — bigger than catch). See 20260906 survey tail.
- Remaining UNSUPPORTED ranking unchanged otherwise (SyntaxTest 12, StructuralUnder 11,
  Pick/Json/IOAPL 9, …; SQL/JavaTypes/Xml 43 out of scope).
- NOT pushed per instruction (commit only; push + branch-invariant sync deferred).
