# PROGRESS-20260830 — `˝` (inverse adverb) repair

**Branch:** `feature/wheres-extra` @ `7606b86` (HEAD before today).
**Invariant:** after fixes → `git branch -f main && git branch -f strings main && git push origin main strings feature/wheres-extra`.

## Gates (green after today)
- `cargo test -p kap-core --lib` → **96 passed / 0 failed**
- `cargo test -p kap-core --test conformance curated_kap_parity` → **1 passed / 0 failed**

---

## DONE today — `˝` inverse adverb (extended)

### (1) Parser drop of `˝` through enclosing paren group  [committed earlier this session]
`bind_operators_kotlin` now postfix-binds a pure adverb even before a delimiter that
closes an *enclosing* group. `((1+)˝) 8` parses as `Apply{Derived{Train[1,+],˝}, 8}`.

### (2) `adverb_inverse` train + scalar-inverse support  [committed earlier]
- `Instr::Train` arm: 2-member `[value, fn]` left-bind → recurse `left=Some(value)`;
  `[f, g]` atop → `g˝(f˝ a)` (Kotlin Chain2.evalInverse1Arg, instr.kt:600).
- `-` self-inverse both forms; `+`/`÷` dyadic left-bound invert with correct operand order;
  `×` dyadic left-bound → `right ÷ left` (Mul evalInverse2ArgB = b÷a); monadic `×` errors.

### (3) `KapNumber::sub` mixed-type operand-order bug  [FIX — pre-existing, broader than ˝]
`sub` (number.rs) combined `(Long,Double)|(Double,Long)` arms and reused `self`'s
binding `a` for the non-`self` operand, so `Double - Long` computed `Long - Double`.
Affected ALL mixed Long/Double/BigInt/Rational/Complex subtraction (e.g. `4.5 - 3`
gave `¯1.5`). Split every combined arm into two with correct `self - other` order.
Kotlin ground truth: `SubAPLFunction.evalInverse2ArgB(a,b)=a-b`,
`evalInverse2ArgA(a,b)=a+b` (math_functions.kt:734/738).

### (4) `⍨` commute inverse in `adverb_inverse`  [FIX — new arm]
Added `Instr::Derived{op:⍨}` arm: `(v f⍨)˝ a = f.evalInverse2ArgA(a, v)`
(Kotlin CommuteFunctionImpl.evalInverse2ArgB/A, commute.kt:24-32; has NO
evalInverse1Arg, so monadic `(f⍨)˝ y` errors). For `+ - × ÷` computed directly:
`+`→`a-v`, `-`→`a+v`, `×`→`a÷v`, `÷`→`a×v`. Nested trains recurse on A-path.

---

## Oracle-verified probe table (port == oracle, numeric)

| Expr | Result | Expr | Result |
|------|--------|------|--------|
| `((1+)˝) 8` | `7` | `(2÷⍨)˝ 6` | `12` |
| `(1+2×3+)˝ 8` | `1/2` | `(-⍨)˝ 5` | **error** (no monadic inverse) |
| `(2×3+)˝ 8` | `1` | `(÷⍨)˝ 2` | **error** (no monadic inverse) |
| `(2-⍨)˝ 3` | `5` | `4.5 - 3` | `1.5` (was `¯1.5` before sub fix) |
| `(2+⍨)˝ 8` | `6` | `3 - 4.5` | `¯1.5` |
| `(2×⍨)˝ 6` | `3` | `+/)˝ 1 2 3 4` | **error** |

Full earlier table (left-bind trains + scalar primitives) is in section "DONE today" of
the first commit this session; all 15 cases match.

## Remaining `˝` gaps (out of scope, logged)
- 3-train **fork** `(A f B)` via explicit fork token (`«»`/fork syntax): Kotlin builds
  `Chain3` which does NOT override evalInverse (no inverse) — port parity not required
  for the everyday space-train, which Kotlin parses as nested atops (verified `1+2×3+`).
- Error-message wording differs in a few spots (port `⍨: Function does not have an
  inverse` vs oracle `⍨: Function does not have an inverse` — same text; `×: Function
  does not have an inverse` matches). Display glyph `¯` vs `-` is cosmetic.


Two defects fixed so parenthesised left-bind trains and scalar primitives invert
correctly, matching the Kotlin oracle (`~/Apps/array/kap-jvm-text`) byte-for-byte.

### Defect 1 — parser silently dropped `˝` through an enclosing paren group
`bind_operators_kotlin` (parser.rs ~1045) treated the enclosing `)` as
`at_statement_boundary()` and broke, silently discarding a trailing adverb.
`((1+)˝) 8` parsed as `Apply{Train[1,+], 8}` (no `Derived`) → returned `9`.
**Fix:** a pure adverb now postfix-binds even when followed by a delimiter that
closes an *enclosing* group; only a genuine terminator (newline/separator/EOF)
strands it. AST now `Apply{Derived{Train[Literal(1), Symbol(+)], ˝}, 8}`.

### Defect 2 — evaluator `adverb_inverse` lacked train + scalar-inverse support
Kotlin ground truth located (READ-ONLY `~/Apps/array/array`):
- Trains are `FunctionCallChain` (instr.kt): `Chain2` (2-train/atop) **implements**
  `evalInverse1Arg = fn1.inv(fn0.inv(a))` (instr.kt:600); `Chain3` (3-train fork)
  does **NOT** → inherits the base-class throw ("Function does not have an inverse").
  `(1+2×)` is a `Chain2` of two left-binds (not a fork) — that is why the oracle
  returns `7/2`.
- `LeftAssignedFunction.evalInverse1Arg(a) = underlying.evalInverse2ArgB(leftArg, a)`
  (functions.kt:628). Scalar rules (math_functions.kt): `+`→`b-a`, `-` self-inverse,
  `×`→`b÷a` (dyadic only; no `evalInverse1Arg`), `÷`→`a×b` (its monadic is reciprocal).

**Fix (`adverb_inverse`, evaluator.rs ~5413-5613):**
- Added `Instr::Train` arm: 2-member `[value, fn]` left-bind → recurse with
  `left=Some(value)`; 2-member `[f, g]` atop → `g˝(f˝ a)`; 3+ member → error.
- `-` now forwards in both monadic and left-bound forms (self-inverse).
- `+`/`÷` monadic forward (identity/self-inverse); dyadic left-bound inverts with
  correct operand order (`+`: `a-left`; `÷`: `left×a` per Div `evalInverse2ArgA`).
- `×` dyadic left-bound → `right ÷ left` (Mul `evalInverse2ArgB` = `b÷a`); monadic
  errors ("Function does not have an inverse").
- Error text aligned to Kotlin: `<fn>: Function does not have an inverse`.

---

## Oracle-verified probe table (port == oracle)

| Expr | Result | Expr | Result |
|------|--------|------|--------|
| `((1+)˝) 8` | `7` | `(÷)˝ 2 4 8` | `(1/2 1/4 1/8)` |
| `(1+)˝ 8` | `7` | `(1+2×)˝ 8` | `7/2` |
| `(2×)˝ 5` | `5/2` | `(1+2×)˝ 2 4 6` | `(1/2 3/2 5/2)` |
| `(10+)˝ 3` | `¯7` | `(1+)˝ 2 3 4` | `(1 2 3)` |
| `(÷)˝ 8` | `1/8` | `(1-)˝ 5` | `¯4` |
| `(÷) 8` | `1/8` | `(-)˝ 5` | `¯5` |
| `((+)˝) 8` | `8` | `(×)˝ 6` | **error** (no inverse) |
| `(+/)˝ 1 2 3 4` | **error** (no inverse) | | |

Note: `(2-⍨)˝ 3` (commute left-bind) returns `5` via the `⍨` path but is out of the
core `˝` scope; not regressed, not specially handled here.

## Remaining
- Broad-sweep conformance delta + KNOWN-NONCONFORMANCE.md (ROADMAP §0.1) — lower priority.

---

## Bitwise `∵` adverb repair (P4-3) — committed after `2b87555`

**Oracle ground truth** (`~/Apps/array/kap-jvm-text`, never ran Gradle). Kotlin
`BitwiseOp` (bitwise_ops.kt:55) calls `fn.deriveBitwise()` per scalar fn; the
resulting operation is the *verified* table below (probed, not inferred):

| glyph | dyadic | monadic |
|-------|--------|---------|
| `×` `∧` | AND `a&b` | error |
| `∨` | OR `a|b` | error |
| `+` `≠` `-` | XOR `a^b` | error |
| `=` | XNOR `!(a^b)` | error |
| `⍲` | NAND `!(a&b)` | error |
| `⍱` | NOR `!(a|b)` | error |
| `<` | `~a&b` | error |
| `>` | `a&~b` | error |
| `≤` | `~a|b` | error |
| `≥` | `a|~b` | error |
| `⌽` | shift (`a`=count, `b<<a`; neg count right-shifts) | error |
| `~` | — | NOT `~b` |
| `⍴` | — | bit-length (`b.bits()`, neg ⇒ `b.inv().bits()`) |
| `⍸` | — | popcount, two's-complement (`popcnt(-1-a)` for neg) |
| `\|` | error "Function does not support bitwise operations" | same |

Monadic: only `~∵ ⍴∵ ⍸∵` defined; every other glyph monadically ⇒
"`∵: Function cannot be called with one argument`". Non-integer operands ⇒
"`∵: Bitwise calls can only be performed on integers`". Monadic ops recurse
**preserving array nesting** (`~∵ (1 2 3)` ⇒ `⟨-2 -3 -4⟩`;
`⍸∵ (0 1 b) (0 0 255)` ⇒ `⟨⟨0 1 4⟩ ⟨0 0 8⟩⟩`).

**Bug found:** the port's `adverb_bitwise` table was wrong — `×∵`/`+∵`/`-∵`
*errored* instead of computing AND/XOR/XOR, and `⍸∵` (popcount) was entirely
unhandled (errored "Function cannot be called with one argument").

**Fix:**
- `adverb_bitwise` (evaluator.rs ~9041): corrected glyph→op table; added `⍸∵`
  two's-complement popcount; made `|∵` error with the right message.
- Added `popcount_bigint` (number.rs:404) — byte-view set-bit count (BigInt has no
  native `count_ones`).
- Added recursive `apply_bitwise_monadic` (evaluator.rs, after `adverb_bitwise`)
  so `~∵`/`⍴∵`/`⍸∵` recurse per-element preserving array shape.

**Verified (numeric, 26 cases):** `99 ×∵ 70→66`, `99 +∵ 70→37`, `99 -∵ 70→37`,
`99 ∧∵ 70→66`, `99 ∨∵ 70→103`, `99 ≠∵ 70→37`, `99 =∵ 70→-38`, `99 ⍲∵ 70→-67`,
`99 ⍱∵ 70→-104`, `99 <∵ 70→4`, `99 >∵ 70→33`, `99 ≤∵ 70→-34`, `99 ≥∵ 70→-5`,
`99 ⌽∵ 70→44367771007988029052384612188160`, `~∵ 13→-14`, `⍴∵ 13→4`,
`⍸∵ 0b1101→3`, `⍸∵ 0 1 0b1111000→⟨0 1 4⟩`, `⍸∵ 255→8`, `⍸∵ ¯1→0`, `2 ⌽∵ 5→20`,
`¯2 ⌽∵ 5→1`, `100 ⌽∵ 5→6338253001141147007483516026880`, `¯99 ∧∵ 70→4`,
`99 ∨∵ ¯70→-5`, `⍸∵ (0 1 0b1111000) (0 0 255)→⟨⟨0 1 4⟩ ⟨0 0 8⟩⟩`. All match
oracle. Residual row diffs are cosmetic (`¯` vs `-` display; error-text wrapping).

**Gates:** `cargo test -p kap-core --lib` 96/0; `curated_kap_parity` 1/0.
**Broad-sweep:** 1541→1549 ok, 764→756 unsupported (coverage 60.5%→60.8%).

**Roadmap correction logged:** P4-2 `⍣` and P4-3 `∵` were marked not-started/missing
but are implemented; `∘∙`/`∙`/`⌻` (P3 outer/inner product) are implemented (the
ROADMAP's "outer product `∘.f`" bucket mislabeled the glyph — `∘.` is
member-deref in Kotlin Kap). Next genuine gaps: `LabelsTest`, `CustomFunctionTest`,
`NumbersTest`, `ReshapeTest` (reshape spec keywords `:match/:fill/:truncate/:recycle`).
