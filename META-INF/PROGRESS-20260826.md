# PROGRESS — 2026-08-26 (rust-kap, OPEN-3 inner/outer product close-out)

**Branch:** `feature/wheres-extra` · **Date:** 2026-08-26 · **Invariant target:** `main == strings == origin/*` after sync.

## [2026-08-26] OPEN-3 — inner/outer product (`f₁ ∙ f₂`) verified complete + curated rows

### Context & resume
Handoff described OPEN-3 as ~95% done with "one bug left": direct form
`1 2 3 ∘∙× 3 4` allegedly failed at eval with "× requires numbers." On resume I
built the binary and **ran the oracle-vs-port matrix first** (hard rule: oracle-first)
rather than trusting the handoff's diagnosis. The "remaining bug" is **already
resolved in the working tree** — `1 2 3 ∘∙× 3 4` returns the correct outer product
`((3 4)(6 8)(9 12))`. The handoff's AST-probe claim (`Apply{fn: InnerProduct{…}, right: Array[3,4]}`
*without* the `1 2 3` left operand) was a stale/theoretical note; the real parser
strands `1 2 3` as the left of the Apply correctly. No eval bug existed.

### Oracle-verified matrix (RUST vs ORACLE, all exact; oracle `┌→───┐`/`⟨⟩` are
box-drawing/renderer glyphs around the same value):

```text
1 2 3 +∙× 1 2 3              O> 14          R> 14
(2 2⍴1 2 3 4) +∙× itself     O> (5 11)(11 25)  R> ((5 11)(11 25))
5 +∙× 1 2 3                 O> 30          R> 30          (scalar normalize)
f ⇐ ∘∙× ⋄ (1 2 3) f (3 4)   O> (3 4)(6 8)(9 12)  R> ((3 4)(6 8)(9 12))
1 2 3 ∘∙× 3 4               O> (3 4)(6 8)(9 12)  R> ((3 4)(6 8)(9 12))   (outer)
1 2 3 +∙× 3 4 5             O> 26          R> 26
1 2 ∘∙× (2 2⍴3 4 5 6)       O> shape ⟨2 2 2⟩  R> (2 2 2)  (outer, matrix right — exact)
```

### Dimension-mismatch error text (matches Kotlin `outer_join.kt:247`)
- Oracle: `Error at: 1:7: ∙: Dimensions of A and B are incompatible. Dimensions of A: [3], Dimensions of B: [2]`
- Port: `error: ∙: Dimensions of A and B are incompatible. Dimensions of A: [3], Dimensions of B: [2]. The size of the last axis of A has to be the same as the first axis of B.`
- The trailing sentence is Kotlin's `details` field (outer_join.kt:249), faithfully kept.
- curated harness cannot assert error text, so the message is documented here, NOT put in a row.

### Changes landed this session
1. `kap-core/tests/conformance.rs` — added 7 curated parity rows for `∙` (inner
   `+∙×` rank-1, matrix inner, scalar-normalize, mismatch length, outer `∘∙×`,
   `f ⇐ ∘∙×`, outer with matrix right) + a comment block noting the error text.
   No behavior change — purely lock-down of already-correct behavior.
2. Rest of OPEN-3 (parser + evaluator InnerProduct machinery) was **already present
   in the uncommitted working tree** from the prior session; this session verifies
   it rather than re-implementing it. No debug instrumentation present (the single
   `eprintln!` at evaluator.rs:594 is the legitimate OPEN-6 `use()` summary —

### Gates (re-run, all GREEN)
- `cargo test -p kap-core --lib` → **96 passed; 0 failed**
- `cargo test -p kap-core --test conformance curated_kap_parity` → **1 passed; 0 failed**
  (run_kotlin_conformance #ignore skipped)
- `cargo build -p kap-cli` → fresh binary; full oracle matrix above matches value-wise.
- `use("math.kap")` + `qr ⇐ {⍵ +∙× ⍵}` evaluates to `<function>` (acceptance case from handoff loads).

### Commit + sync
Commit on `feature/wheres-extra`, then restore `main == strings == origin/*`.

## [2026-08-26] Next phase queue (after OPEN-3 close)
Per ROADMAP §6: remaining phases are P6 (done), P7 (stdlib chain — stat.kap median
still diverges, tracked in PROBLEM.md #2), P8 (renderer `()` vs `⟨⟩` `Str`-vs-char-array
modeling gap). No new work started this session — OPEN-3 was the carried-over item.
