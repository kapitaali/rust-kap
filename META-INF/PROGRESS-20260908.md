# PROGRESS-20260908.md — rust-kap feature/wheres-extra (2026-09-08)

> Per-day file per ROADMAP §0.1 law 8. Previous day: PROGRESS-20260907 (left-bind
> 2f4a15f, :constant/:exprfunction ebfbbf6, 91.6%). Session arc since: 4 commits
> to 94.0% (490aa98, 4a71d76, 08f1b76), handoff synced at 57afd7c.

**Branch:** `feature/wheres-extra`. **HEAD at session start:** `57afd7c` (clean).
**Gates (re-run):** lib 111/0, curated 1/0. **Baseline sweep (release):**
2482 OK / 0 MISMATCH / 159 unsupported = 94.0% (2641 cases, `/tmp/dump96-base.txt`).

## Goal: 96% (+53 net OK, 0 MISMATCH)

159 unsupported triaged from dump rows (positional vs `kotlin_tests.jsonl`):

Out of scope (~70, JVM libs/files/stdlib harness never loads — skip): SQL 21,
JavaTypes 13, Json 9 (no `json:` ns), IOAPL-file 9 / Csv 5 (need `test-data/`
files — see T4), Xml 8, time 7, JvmMethodCalls 5, Calcite 4, Lock 4,
html/arrow/encoder, stdlib-only (`⌹`, `io:`, `math:pi`?, `o3:`, `s:col`),
`${…}` extractor artifacts (3 CompareTest rows — expr is literally
`${value} ${name}…`, unfixable), EncodeTest big-int `⊤` (do-not-touch),
`math:sin/⍬` (oracle errors), `⊃ 200000⍴…` (OOM-type row).

In-scope attack order (exp-bearing rows first — genuine wins):

- T1. `⍫` obverse (+2: rows 680, 681). Kotlin `ObverseOp` (engine.kt:503) +
  `ObverseFunction` (op.kt:304): forward=fn0, inverse=fn1. Port has ZERO `⍫`
  refs. Shape: `Instr::Obverse{left_fn,right_fn}` mirroring `OverOp`
  (ast.rs:164); Symbol-based bind arms mirroring `∙` (parser.rs ~1342);
  eval forward block mirroring OverOp (evaluator.rs ~3147); inverse arms in
  `adverb_inverse` (direct Obverse + Symbol-alias-to-Obverse-UserFn, else
  `(bar˝ 100)` dies "no inverse"); `apply_user_fn` body route + FnAssign
  store-direct + `is_function_expr` + `is_two_arg_operator`.
- T2. `+[k]/` functionAxis on nested data (+2 rows 2085/86, maybe OperatorsTest
  2002/03 `{,[1]/⍵}`/`{,[⍺]/⍵}` + ComposeTest 1517). Port validates the axis
  against enclosed rank-1 (evaluator.rs ~13416) and rejects `+[1]/…`; Kotlin
  `AxisValAssignedFunctionDirect` threads axis as fold-step `functionAxis`
  (reduce.kt `eval2Arg…WithAxis`), no enclosed-rank check. Oracle row 2085:
  `+[1]/ (1 2) (2 2 ⍴ 3 4 5 6)` → dims (2 2) content (4 6 6 8).
- T3. `√⍨˝` commute-inverse (row 1984, exp=None). Commute-inverse arm handles
  `⋆` A-path already; add `√`: solve `v √ t = a` ⇒ `t = a⋆v`.
- T4. `namespace("foo")` call-form + `use("test-data/…")` (rows 1896, 1904,
  1905, 1182, 1183): vendor Kotlin `test-data/` files under `kap-core/test-data/`
  (harness CWD finds basenames via `use_file` CWD dir); `namespace("foo")`
  must create/switch namespace and `'bar` evaluate in it. IOAPL/Csv rows reuse
  the same vendored files if `io:` builtins resolve CWD-relative.
- T5. Singles (probe each vs oracle first): `math:pi` (1985),
  `io:toHex`/`fromHex` (1003-05), `(:multiLine regex:compile …)` (2112/13),
  `declare(:singleCharExported…)` (2346), `map:appendTo` (1887/88),
  `a⍠valuePlusN` method-call (640/41), `2 1 3 1 ,/⌸…` (442),
  `deferInOperator` (128), `capturedFrameWithCollapse` (894),
  `leftBindWithScope` (1527), `parseListWithStatements` (1831),
  `(⊃ int:proto 1000)[2]…` (1686), `throwNative` (1054),
  `inverseFunctionEscapeScope` λ-escape (929), structural-under closure (1044),
  ScopeTest 914/921/926, RankTest 864, LabelsTest s:col 505.

Stop rules: parser-blast-radius tasks stop at 2 failed attempts; one cluster
per commit; 0-MISMATCH invariant; gates + sweep diff per commit.
