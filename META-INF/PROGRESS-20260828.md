# PROGRESS-20260828 — `⟦⟧` (FunctionCallOpenParen) parser support

**Branch:** `feature/wheres-extra` @ `95810e6` (gates green after today's fix).
**Invariant:** after fixes → `git branch -f strings main && git push origin main strings`.

## Gates (current, green)
- `cargo test -p kap-core --lib` → **96 passed / 0 failed**
- `cargo test -p kap-core --test conformance curated_kap_parity` → **1 passed / 0 failed**
  (the `run_kotlin_conformance` panic is FAILED-by-design; ignore it.)
- Build gate: `cargo build -p kap-cli` (not `-p kap-core` alone) after any parser edit.

## Oracle (ground truth)
`~/Apps/array/kap-jvm-text/bin/kap-jvm-text --lib-path=$HOME/Apps/array/kap-jvm-text/standard-lib`
Result line: `⊢ …`. `~/Apps/array/array` (Kotlin) is READ-ONLY — never run; use JVM oracle.

---

## DONE today

### `⟦ … ⟧` function-call list fully implemented (task t4)

`⟦` = Kotlin `FunctionCallOpenParen` (tokeniser.kt:480); `⟧` = `FunctionCallCloseParen`.
Handled only in `processFn` (parser.kt:440): a function immediately followed by `⟦`
parses `;`-separated contents into a single LIST argument → **monadic** call
`fn (a b c)`. It is NOT a standalone bracket and NOT dyadic (oracle: `g ⇐ {⍺+⍵} ⋄ g⟦10;20⟧`
→ "Variable not assigned: kap:⍺", proving ⍵=(10 20), ⍺ unbound).

Oracle verdicts (verbatim):
- `g ⇐ {⍵} ⋄ g⟦1;2;3⟧` → `⊢ ╔═╤═╤═╗`
- `g⟦⟧` → `⊢ ╔⊖╗` (empty list)
- `g⟦42⟧` → `⊢ 42`
- top-level `⟦+;1;2⟧` → `⊢ Error at: 1:1: Unexpected token: FunctionCallOpenParen`

Files edited: `kap-core/src/token.rs`, `kap-core/src/lexer.rs`, `kap-core/src/parser.rs`.

1. **token.rs** — added `FunctionCallOpenParen` / `FunctionCallCloseParen` variants
   (after `MemberDereferenceToken`).
2. **lexer.rs** `single_char_token` — `'⟦' => FunctionCallOpenParen`,
   `'⟧' => FunctionCallCloseParen`.
3. **parser.rs** new helper `parse_function_call_list(&mut self, fn_instr: Instr)`:
   consume `⟦`; push `FunctionCallCloseParen` on `kotlin_close_stack`; loop `parse_expr()`
   items separated by `;`; empty `⟦⟧` → empty `Array`; returns
   `Apply { fn_expr, left: None, right: Array[elements] }` (monadic). On error, pop the
   close stack before returning. Borrow-checker fix (E0502): extract `next_kind: Option<Token>`
   into an owned value before `kotlin_close_stack.pop()`.
4. **parser.rs `finish_fn_call`** — after `bind_operators_kotlin`, if next token is
   `FunctionCallOpenParen`, delegate to `parse_function_call_list`.
5. **parser.rs `parse_apply`** — after `first = parse_primary()`, if `is_function_expr(first)`
   && next is `FunctionCallOpenParen`, `return parse_function_call_list(first)` (fixes the
   paren-group form `(g⟦a;b⟧)` which formerly routed through legacy `parse_apply`).
6. **parser.rs `parse_primary`** — `FunctionCallOpenParen` arm →
   `Err("unexpected token: function-call bracket ⟦ (must follow a function)")`
   (top-level `⟦` error, matches oracle intent).
7. **parser.rs `parse_value_kotlin`** — `LambdaToken` arm: advance, `parse_function_atom`,
   `finish_fn_call` (fixes `λ` inside a `⟦` list — was hitting the catch-all reset loop).

### Bug found & fixed during probing

In `parse_apply`'s leading-symbol monadic-apply heuristic, `next_is_operand_not_fn`
did NOT exclude `Token::FunctionCallCloseParen` (nor `FunctionCallOpenParen`). A defined
function symbol (in `known_functions` via `⇐`) followed by `⟧` was treated as a monadic
operand and `parse_apply()` was called on `⟧` → "unexpected token in primary".

- Only tripped for **defined** symbols as `⟦` elements (undefined → `is_known` false → strand;
  literals → Symbol branch skipped).
- Fix: added `&& !matches!(t, Token::FunctionCallCloseParen)` and `FunctionCallOpenParen`
  to the operand-exclusion list, so a leading function inside `⟦ … ⟧` stops at `⟧`.

### Verification (port vs oracle, value-equivalent)
- `g⟦1;2;3⟧` → port `(1 2 3)` | oracle boxed frame (display-only divergence, KNOWN-NONCONFORMANCE class).
- `g⟦⟧` → port `()` | oracle `╔⊖╗` (empty list).
- `g⟦42⟧` → port `(42)` | oracle `42`.
- `(g⟦1;2;3⟧)` → port `(1 2 3)` (paren-group form fixed).
- `f ⇐ {⍵} ⋄ f⟦g⟦1;2⟧;3⟧` → port `((1 2) 3)` (nested call).
- `f ⇐ {⍵} ⋄ f⟦a;b⟧` with `a`/`b` undefined → `error: undefined symbol: a` (correct runtime error).
- `fhelp.kap:11` form `fhelpCmd ⇐ {⍵} ⋄ int:registerCmd⟦"man";"Help";λfhelpCmd⟧`
  → **no parse error** (was the original t4 failure). `int:registerCmd` itself is a
  separate not-yet-implemented stdlib fn → eval error, not a parse failure.

### t4 closeout
`fhelp.kap` now **parses** on the port. The remaining `unknown function: int:registerCmd`
is a stdlib-eval gap (that native fn is not implemented), outside the parser task.

---

## Remaining (backlog)

- **t6b (PARTIAL)** — `(a;b;c)←1 2 3` silently strands instead of erroring. Requires `APLValue::List` — a large refactor (~816 match sites, 7 files). See KNOWN-NONCONFORMANCE.md.
- **t6a (PARTIAL)** — Non-leading `⍥` with data operands (e.g. `(1 2 3)⍥,(4 5 6)`) still errors. Separate pre-existing divergence.
- Record the boxed-list DISPLAY divergence in `META-INF/KNOWN-NONCONFORMANCE.md` — **DONE**

---

## 2026-08-28 continued — P0 conformance repairs

### P0.1 stale curated rows — ALREADY DONE
- `"3 | 2"` → `"2"` (was `"1"` before fix)
- `"5 ∊ 1 2 3 4"` → `"0"` (was `"(0)"` before fix)

### P0.2 `use()` error policy — DONE
**Fix:** `eval_string_in_env_tolerant` now **aborts at the first failing statement** and propagates the error to the caller. Earlier definitions persist; later ones do not.

Oracle-verified behavior:
```
a ← 1 / b ← undefined / c ← 3
```
- `use("file")` → `Error at file:2:5: Variable not assigned: default:undefined`
- `a` is defined, `c` is NOT defined (later statements skipped)

Files: `kap-core/src/evaluator.rs` (lines ~558-595)

**Side effect:** now exposes that `math.kap` itself triggers `Assignment to constant variable: math:pi` — this is **correct oracle behavior** (oracle also errors). The stdlib loading failure is P6 (native quad constants).

### P0.3 ERRORS.md — EXISTS
`META-INF/ERRORS.md` has the operator/namespace/dimension exception table. Key rows:
- `InvalidOperatorArgument` → `Operator without left function: X` ✓
- `IllegalContextForFunction` → `No arguments specified for function` ~ (port says "Operator without left function")
- constant assignment → `Assignment to constant variable: ns:name` ✓
- unassigned variable → `Variable not assigned: ns:name` ~ (port says `undefined symbol`)

### P0.4 `group_indices` panic — ALREADY GUARDED
`arrayify_value()` at line 6389-6390 protects `b_dims[0]` indexing. Scalar `⫇` scalar works (`(⍬ (5))`). No panic.

### P0 acceptance: ALL GATES GREEN
- `cargo test -p kap-core --lib` → **96 passed / 0 failed**
- `cargo test -p kap-core --test conformance curated_kap_parity` → **1 passed / 0 failed**

---

## Current roadmap position

**Next: P1 — parser architecture migration** (highest-value structural work)

The port's heuristic valence parser diverges from Kotlin's single-pass accumulator loop. Every recurring parser burn (unary-minus swallowing, invented dyadic forms, eager operator execution) traces to guessing where Kotlin accumulates. ROADMAP §P1 has the detailed breakdown.
