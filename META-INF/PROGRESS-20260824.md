# Progress — 2026-08-24

> GROUND TRUTH: Kotlin source (`~/Apps/array/array`) + oracle
> (`~/Apps/array/kap-jvm-text/bin/kap-jvm-text --lib-path=$HOME/Apps/array/kap-jvm-text/standard-lib`).
> Never claim behaviour without a captured transcript.

## Session start

Resumed at commit `d65cb39` (P1-M6 complete: Kotlin accumulator parser is the
default; `KAP_KOTLIN_PARSER=0` opts out). Baseline gates GREEN:
- `cargo test -p kap-core --lib` → 92 passed; 0 failed
- `cargo test -p kap-core --test conformance curated_kap_parity` → 1 passed; 0 failed

Today's target (handoff priority #1): **block-body unification** —
1. Port the keyword-block (`if`/`while`/`when`) and ∇ statement arms into
   `parse_value_kotlin` (the M5b precondition).
2. Re-attempt M5b: dispatch dfn `{…}` interiors through the accumulator loop.
3. Stdlib sweep legacy-vs-default; document or revert.

## [2026-08-24] ROADMAP P1 — M7 COMPLETE: block-body unification (M5b retried, now GREEN)

### What landed (commit `0172d93`, all in kap-core/src/parser.rs)

1. **Statement arms ported into `parse_value_kotlin`** (the M5b precondition):
   - `Token::FnDefSym` arm = Kotlin parser.kt:999 → `processFunctionDefinition` (:571)
     port: non-empty leftArgs ⇒ parse error "Function definition with non-null left
     argument"; otherwise `parse_fn_def()` (which consumes ∇) and return.
   - `if`/`while`/`when` symbol arm = parser.kt:1014–1015 (`processIf`/`processWhile`),
     guarded identically to `parse_keyword_prefix` (incl. defsyntax-overrides-`when`)
     then delegated to it.
   - **Both RETURN directly instead of appending to left_args.** First attempt appended
     and kept looping; at top level the loop's `skip_newlines()` eats the Newline that
     Kotlin's END_EXPR_TOKEN_LIST (:1342) would terminate on, so a following line's ∇
     arrived with the previous statement still accumulated → io.kap regressed 0→4
     warnings. The direct-return form matches observable behaviour.

2. **M5b-2 block dispatch**: `parse_block` statements now go through
   `parse_value_kotlin` with PER-STATEMENT legacy fallback (restore pos on any error,
   retry via `parse_expr`) — not M5b's whole-block bailout. Mixed bodies work.

3. **Short-form fn defs register at parse time**: the `name ⇐ rhs` lookahead arm now
   pushes the name into `known_functions`, mirroring Kotlin `processShortFormFn`
   (defines during parse). Fixes `{g ⇐ {⍵+10} ⋄ g ⍵} 5` (was stranding `(g ⍵)`).

4. **`is_function_expr` admits `Instr::Block`**: a `{…}` dfn is a function value
   (Kotlin OpenFnDef → processFn). Fixes pre-existing `{2×⍵}¨ 1 2 3` →
   `undefined symbol: ¨`; now `(2 4 6)` == oracle `⟨2 4 6⟩`.

### Oracle verification (all fresh, side-by-side)
- Statement level: if/else=11 · x←if=11 · while-sum=6 · when=10 · `∇ f (x)`=6 ·
  `∇ (y) g (x)` dyadic op-style=12 — all match oracle.
- Dfn interiors: `{x ← ⍵+1 ⋄ x*2} 5`=36 · `{+/⍵}`=10 · `{if…}`=100 · `{when…}`=1 ·
  while-in-dfn=10 · `{⍺←99 ⋄ ⍵+⍺} 1`=100 · `{g ⇐ …}`=15 · `{2×⍵}¨`= (2 4 6).
- P1 non-regression matrix re-probed green: `(10+) 1`=11 · `(1↑⍴) 3 4`=2 ·
  `:hello :sir` strands · `⍎"1/2"`=1r2 · `foo ⇐ ×-` · `1 2 {≢⍵}¨ 3 4`.
- Known-divergence class unchanged: `{a b ← ⍳2}` errors on both engines (text
  differs: port "undefined symbol" vs oracle "Can only assign to a single variable"
  — MSG-class item); `{f ← + ⋄ f ¨ ⍵}` errors identically to oracle.

### Gates + sweep (after ALL edits)
- `cargo build -p kap-cli`: clean.
- lib: **92 passed; 0 failed** · curated: **1 passed; 0 failed**.
- Stdlib sweep (warnings, default vs legacy): io 4→**0**, math 26→**0**,
  math-kap 26→**0**, output3 152→128 (legacy 131), standard-lib 233→184 (legacy 185),
  util tie; http 7 vs 6 and stat 13 vs 12 marginally worse (pre-existing constructs,
  not new failure classes).

### Status
M7 closes handoff priority #1 (block-body unification). Legacy heuristics remain
reachable via KAP_KOTLIN_PARSER=0 opt-out only; deletion can now be scoped.
Next candidates: http/stat ±1 deltas, multi-operand-← error text alignment, P2.
