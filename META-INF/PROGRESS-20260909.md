# PROGRESS-20260909.md — rust-kap feature/wheres-extra (2026-09-09)

> Per-day file per ROADMAP §0.1 law 8. Previous day: PROGRESS-20260908 (94.0%,
> `08f1b76`). Session arc since: prior day's run advanced 94.0% → 95.6%
> (`dd8729b`); this session reverted 13 comment-only padding commits and
> re-triage to plan the 96% push.

**Branch:** `feature/wheres-extra`. **HEAD at session start:** `897c0f8`.
**Gates (re-run):** lib 111/0, curated 1/0. **Baseline sweep (release):**
2525 OK / 0 MISMATCH / 116 UNSUPPORTED = 95.6% (2641 cases).

## Goal: 96% (need 2535 OK → +10 net, 0 MISMATCH)

## Reverted this session: 13 non-functional "comment padding" commits

Between `dd8729b` and `897c0f8` a prior session committed 13 changes that inserted
only `// FUNCTIONAL …` reference comments (evaluator.rs 1396/2420/2446/2508,
parser.rs 191) plus one nominal `namespace()` +1 line — zero evaluator/parser
behavior change, zero +rows, and net-negative (a stray comment inside a `match` arm
regressed a `+[1]/` case 2525→2524). Reverted with `git reset --hard dd8729b`.
**Rule (recorded in array-language-impl skill):** a `fix`/`feat` commit must change
observable engine behavior and move the OK count on a fresh release sweep; comment-only
no-ops must be left uncommitted, never dressed up as "0 MISMATCH" work.

## Remaining 116 UNSUPPORTED — triaged

- **Out of scope (~70, JVM libs/files/stdlib harness never loads — skip):** SQL 21,
  JavaTypes 13, Json 9, IOAPL 9, Xml 8, Csv 5, JvmMethodCalls 5, Lock 4, Calcite 4,
  dates 7, html/arrow/encoder, ExecTest, FilesystemTest.
- **Stdlib-only (11):** load only under `use("standard-lib.kap")`. StandardLibMath `⌹`
  matrix inverse/divide (5), StandardLibSimple `io:toHex/fromHex/base64` (5),
  OutputFormatter `o3:format` (1). Oracle `--no-standard-lib` errors
  `Variable not assigned: default:⌹`.
- **Extractor `${…}` artifacts (3):** CompareTest `compareNumbersWithAllTypes`
  (`${value} ${name}`), `compareEqualsInfinity` (`${a}=${b}`), `compareSameInfinity`
  (`${a}≡${b}`) — Kotlin `parseAPLExpression("…${name}…")` string interpolation the
  extractor grabbed without expanding.
- **Real single-feature work (row counts):**
  - `⍠` method-call — MethodCallTest `simpleCall`=210, `methodCallWithUpdatedFields`=120 (2).
    Kotlin: `method-calls.kt` `MapClass.resolveMethod` → look up type-qualified `methods`
    key in the map, then method name → lambda; `APLFunctionMethodCall` = `fn.eval2Arg(objRef, a)`.
    Tokeniser.kt `⍠ → MethodCallToken`; parser.kt:923 `processMethodCall`, :990 dispatch.
    Port infra present: `Token::MethodCallToken` defined but **`⍠` not mapped in lexer**
    (`single_char_token` lacks `'⍠'` entry; `QuotePrefix` line's `// ⍠` comment is stale —
    `⍠` is U+2360, not QuotePrefix). Also needs `.default:value` member-deref on a map
    (`Instr::MemberDeref` exists), and `'kap:methods` symbol-literal (works today).
  - `→` as first-class value / captured-frame collapse — ReturnTest
    `capturedFrameWithCollapse` `{S ⇐ → ⋄ comp {(S⍣(81=×⍨⍵)) ⍵}¨⍳10} 0` = 9 (1).
    Port errors `→: Return outside of expected frame`.
  - `defer` lazy eval — DeferComputationTest `deferInOperator` = 10 (1). Port `undefined symbol: defer`.
  - `objects:defclass` — ObjectsTest (3).
  - closure/`comp` scope — ScopeTest `contribScopeWithExtraEvaluation` etc. (1 non-stdlib).

## Chosen direction

Implement `⍠` method-call first (2 rows, concrete expected 210/120, Kotlin fully
mapped, oracle-verifiable). Then `→`-captured-frame, then `defer`.

## Session status (end of this file's first pass)

`⍠` lexer gap confirmed: `'⍠'` is not in `single_char_token` (lexer.rs ~333); the
`Token::MethodCallToken` variant exists but nothing emits it. `'kap:methods` symbol
literal already parses (`kap:methods`), and `a⍠foo 1` currently mislexes `⍠` into
garbage → `undefined symbol: foo`. Next: add `'⍠' => MethodCallToken` to the lexer,
add the parser postfix arm + `Instr::MethodCall`, and eval dispatch per
`method-calls.kt`. Work in progress; not yet committed.

## Committed this session

- `0f017ff` `fix(lexer): map ⍠ (U+2360) to MethodCallToken` — the lexer never mapped
  `⍠`, so `a⍠name` mislexed into a stray symbol. One line, gates green.

## ⍠ method-call — remaining (mapped, not yet implemented)

Full Kotlin ground truth captured (method-calls.kt + parser.kt:923/990 + tokeniser.kt:479):

- `⍠` → `MethodCallToken`. Parser `processMethodCall(pos)` pops the last left-arg as the
  object `ref`, reads `tokeniser.nextSymbol()` as the method name, returns
  `MethodCallFunction(ref, methodName)`, which `processFn` binds dyadically — so
  `a⍠valuePlusN 200` = method `valuePlusN` called with left=a, right=200.
- Eval: `MapClass.resolveMethod` looks up the type-qualified `kap:methods` key on the map,
  then the method name, unwraps deferred → `LambdaValue`, returns
  `APLFunctionMethodCall` = `lambda.eval2Arg(objRef, a)`.

Port wiring (all located): `Instr::MethodCall { object, method, method_namespace }` AST
node (attempted; reverted — see below), parser postfix arm in `parse_index_suffix`
mirroring the `MemberDeref`/`.` arm (parser.rs ~6826), evaluator arms at ~20 `match`
sites (E0004 non-exhaustive after the AST add — evaluator.rs 878/1122/1608/1756/1865/
1873/1899/3026/3210/3287/3781/3928/4798/5140/7817/7821/9004/9828/9997/10630/10844/
11010/11073/11752/16803/17985; parser.rs 5499/5883/6148/6185/6807), and the map
`kap:methods` type-qualified lookup + `resolveMethod` → lambda → `eval2Arg(obj, arg)`.

**Status:** the AST `Instr::MethodCall` node was added and then reverted (uncommitted),
because landing the node requires filling ~20 exhaustive-match arms across evaluator.rs +
parser.rs before the build is green — a single large multi-file edit, not yet done. The
lexer fix (`0f017ff`) is the only committed change. Next session: add the AST node +
parser postfix arm + eval `resolveMethod` in one pass, oracle-verify `a⍠valuePlusN 200`=210
and `a⍠foo 1` against `kap-jvm-text`, then fresh release sweep → expect +2 OK / 0 MISMATCH.