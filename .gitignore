# Rust build artifacts (large, regenerated on every build)
/target

# Conformance harness output (generated by `run_kotlin_conformance`)
/conformance_summary.txt

# Scratch work dir (local probes, dumps)
/work/

# OS / editor cruft
.DS_Store
*.swp

# Internal progress docs — not part of the conformant release
/META-INF/

# Local planning doc (untracked for now)
/20260910_RENEWED_ARCHITECTURE.md

# Convenience symlink to target/debug/kap (created for RIDE manual mode)
/kap
