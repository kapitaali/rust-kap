namespace("kap")

∇ (keys) (fn ⌸) (values) {
        keyindex ← =keys
        ((≠keyindex)/keys) ,[0.5] ⍞fn¨ keyindex⫇values
}

declare(:export ⌸)

⎕p ⇐ (⊣ io:print)
⎕pl ⇐ (⊣ io:println)

declare(:export (⎕p ⎕pl ⎕A ⎕a ⎕d))
